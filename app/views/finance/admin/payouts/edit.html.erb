<div class="ui segment breadcrumb">
  <%= link_to 'Back', finance_payouts_path(id: @payout.id), class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Edit</div>
</div>

<div class="ui segment">
  <%= render 'form' %>
</div>