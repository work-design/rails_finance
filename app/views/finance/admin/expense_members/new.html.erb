<div class="ui segment breadcrumb">
  <%= link_to 'Back', finance_expense_expense_members_path(@expense), class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Add</div>
</div>

<div class="ui segment">
  <%= render 'form' %>
</div>