<% form_object = form_object(@budget) %>

<% params.fetch('budget', {}).fetch('expense_items_attributes', {}).each do |index, item| %>
var template = document.createElement('div')
template.innerHTML = '<%= j(render partial: 'item_field', locals: { f: form_object, index: index }) %>'
document.getElementById('budget_expense_items_wrapper_<%= ef.index %>').parentNode.replaceChild(input, template)
<% end %>
