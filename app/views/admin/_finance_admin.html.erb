<div class="media" data-controller="empty">
  <figure class="media-left">
    <%# image_tag meta[0].logo, class: 'image is-64x64' if meta[0].logo.present? %>
  </figure>
  <div class="media-content">
    <div class="content">
      <strong><%= t('finance.admin.title') %></strong>
    </div>
    <div data-empty-target="content">
      <%= link_to({ controller: 'finance/admin/expenses' }, class: 'tag') do %>
        <span><%= t('finance.admin.expenses.index.title') %></span>
      <% end %>
      <%= link_to({ controller: 'finance/admin/financial_taxons' }, class: 'tag') do %>
        <span><%= t('finance.admin.financial_taxons.index.title') %></span>
      <% end %>
    </div>
  </div>
</div>
